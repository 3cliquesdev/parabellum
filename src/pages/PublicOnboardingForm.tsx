import { PremiumOnboardingForm } from "@/components/premium-onboarding/PremiumOnboardingForm";

export default function PublicOnboardingForm() {
  return (
    <div className="h-screen flex flex-col overflow-hidden bg-slate-50 dark:bg-background">
      <PremiumOnboardingForm />
    </div>
  );
}
